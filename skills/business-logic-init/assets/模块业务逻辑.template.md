---
模块: <kebab-id，与文件名一致，如 order>
名称: <业务模块中文名>
状态: 待补全            # 待补全 | 草稿 | 已核对
涉及端: [<小程序>, <后台管理>, <后端>]
最近更新: <YYYY-MM-DD>
关联模块: [<other-id>]   # 会互相影响的模块 id
关联计划: <../plan/xxx-实施计划.md，模式③收尾时填>   # 轻量溯源：本模块最近一次由哪份计划改动
关联提交: <commit 短哈希，模式③收尾时填>              # 轻量溯源：对应哪次代码提交
---

> 每节都要填；本模块没有的内容明写「无」，不要留空——留空分不清是"没有"还是"忘了写"。写作颗粒度见技能 `references/module-doc-spec.md`。

# <业务模块中文名> 业务逻辑

## 一、这个模块负责什么（一段话）
> 用**一段话**（3-5 句）讲清业务边界——不是标题，是概要。写三件事：
> 1. **业务领域**：这个模块在业务上管哪一块
> 2. **核心业务对象**：涉及哪些核心概念（如订单、库存记录、佣金记录），它们之间什么关系
> 3. **主要业务操作/流程**：涵盖哪些关键业务动作或流程（如"下单→支付→发货→退款"）
>
> 写完后自检：一个没看过代码的人，读完这一段能不能大致知道这个模块是干什么的？不能就继续写。不要怕写多——怕写少。

## 二、用户操作与页面响应（前端 / 各操作端）
> 逐个「用户能做的动作」写清：**怎么操作 → 有什么效果 → 成功怎么展示 / 失败怎么展示 / 其它状态怎么展示**。用大白话，把真实体验写出来。

### 操作 1：<如「提交订单」>
- **操作方式**：<点击 / 拖动 / 滑动 / 长按 / 直接展示（无需操作）/ 表单输入后点击>
- **触发效果**：<点了之后系统做什么>
- **成功展示**：<成功后页面怎么变——弹提示？跳转？刷新列表？>
- **失败展示**：<按类型分——校验失败 / 业务失败(库存不足等) / 权限失败 / 系统失败(500超时) 分别展示什么>
- **边界/异常状态**：<空态 / 加载中 / 无权限或未登录 / 重复提交连点 / 并发抢占 / 网络超时断网 / 数据已过期 时分别展示什么>

### 操作 2：<…>
（同上结构，一个动作一段）

## 三、后台管理对应操作（后台管理端）
> 这块业务在后台管理端要有哪些管理能力。

- **菜单位置**：<在后台哪个菜单下>
- **增删改查**：<能不能新增 / 编辑 / 删除 / 列表查询筛选；各自的业务约束>
- **审核 / 流转**：<是否需要人工审核、上下架、发布/驳回等>
- **权限管理**：<哪些角色能操作、需要什么权限点>

## 四、后端处理逻辑与业务规则
> 用户/后台操作后，后端在业务上怎么处理、有哪些规则和判断。纯文字讲清「系统怎么算、怎么判、怎么流转」。

- 规则 1：<如「同一活动每人限购 N 件，超出下单失败并提示已达上限」>
- 规则 2：<…>
- 边界/异常：<空态 / 并发 / 重复提交 / 超时 / 权限不足 时，业务上怎么处理>

## 五、各端接口清单
> 每端调哪些接口，接口元信息写全。路径/方法/参数以真实代码/Swagger 为准。限流、版本没有就写「无」。

| 端 | 路径 | 方法 | 需登录 | 需权限 | 请求参数(名+位置) | 限流/幂等 | 版本 | 说明 |
|----|------|------|--------|--------|-------------------|-----------|------|------|
| 小程序 | `/api/app/order/create` | POST | 是 | 否 | `productId,qty,addressId`(body) | 幂等键 | 无 | 提交订单 |
| 后台管理 | `/api/app/order/list` | GET | 是 | 订单.查看 | `status?,keyword?,page`(query) | 无 | 无 | 订单列表 |
| … | | | | | | | | |

## 六、关联数据表
> 只列表名等于没写——关系/外键/级联才决定"改这块会不会连累别处"。以真实数据库/实体为准。

| 数据表 | 存什么 | 关联关系 | 外键/唯一约束 | 级联策略 |
|--------|--------|----------|---------------|----------|
| <Orders> | <订单主记录> | <Order 1:N OrderItem> | <UserId 外键> | <软删> |
| <OrderItems> | <订单明细行> | <N:1 Order> | <OrderId 外键> | <随订单级联删> |

## 七、影响到的功能
> 这个模块一旦改动，会牵动哪些别的功能，以及**为什么会牵动**。

| 受影响功能 | 影响原因 |
|-----------|----------|
| <库存> | <下单要扣库存，改下单逻辑会影响库存计算> |
| <[[coupon]] 优惠券> | <下单时核销优惠券> |

## 八、状态流转（如有）
> 业务对象有状态就填；没有写「无状态流转」。

| 当前状态 | 事件/条件 | 目标状态 |
|----------|-----------|----------|
| 待支付 | 支付成功 | 待发货 |

## 九、未决问题 / 待人工确认
- [ ] <代码里没确认清楚、需要人拍板的点>

## 溯源
- 抽取自：<源码路径 / 设计稿 / 旧系统>
- 最近核对：<YYYY-MM-DD>
