# <业务/功能名称> - 需求文档（从现有系统抽取）

> **文档状态**：草稿 / 待确认 / 已核对　（选一；用户拍板前不许写「已核对」）
> **业务模块**：<模块/功能名>　**抽取日期**：YYYY-MM-DD　**最近核对日期**：YYYY-MM-DD
> **用途与读者**：<业务评审 / 验收 / 指导其他 AI 在自己项目实现……>
> **一致性**：本交付文档**零实现标识**（不含原代码字段名/变量名/表名/接口路径/文件名等）；核对证据见同目录《<日期>-<业务名>-一致性核对底稿.md》（含代码内部标识，仅供核对，禁止并入正文）。
> **关联**：影响面用 [[模块id]] 标注（如有白皮书体系）。

---

## 〇、抽取范围与前提限定
- 抽取覆盖：前端（页面/校验/状态）□　后端（规则/状态/任务）□　数据库（约束）□　测试 □　配置 □
- 纳入功能：<逐项列>；排除功能：<逐项列，或「无」>
- 前提限定：<对本文档有效性的假设，如「某处行为依赖系统配置，需人工确认」>

## 一、业务概述
3–5 句讲清：① 业务领域　② 核心业务对象与关系（业务名 + 一句话定义）　③ 主要操作/流程。没看过代码的人读完应知道这模块干什么。

## 二、术语表
| 术语 | 定义 | 备注 |
|------|------|------|
| <询价单> | <买方一次性采购需求的单据> |      |

（与项目 CONTEXT/白皮书术语对齐；有冲突先指出再对齐）

## 三、用户与角色 / 权限
哪些角色、哪些端（前台/供应商/后台…）、各角色能做什么、后台操作由谁触发。

## 四、用户交互流程
每个「用户能做的动作」写四类结局：怎么操作 / 触发什么 / 成功如何展示 / 失败如何展示（失败分 校验/业务/权限/系统）。
边界与异常：空态、加载、未登录/无权限、重复提交/连点、并发、超时/断网、数据过期。

## 五、系统处理链路（业务语言）
从「用户操作」到「业务结果」的处理链：校验 → 状态流转 → 规则执行 → 结果反馈 → 异常恢复。
真实发生的技术保证写成业务约束，如「系统在数据层保证：同一询价单对同一供应商只存在一次报价」。

## 六、业务规则清单
| 编号 | 规则（业务语义） | 触发条件 | 结果 / 例外 | 硬/软 | 核对状态 |
|------|------------------|----------|-------------|-------|----------|
| R1 | <仅「草稿」状态可提交询价单> | <买方点击提交> | <通过进入「待报价」；失败给提示> | 硬 | ☑ 一致 |
| R2 | <未通过认证或已被拉黑的供应商不能报价> | <供应商提交报价> | <拒绝并提示原因> | 硬 | ☑ 一致 |
| R3 | <同一询价单对同一供应商全局只能存在一次报价> | <提交报价> | <重复提交被系统拒绝> | 硬 | ☑ 一致 |
| R4 | <金额满 300 可用 20 元满减，每单最多一张> | <成交前计算> | ⚠ 前端宣传为满 200，与后端不一致——待确认以谁为准 | 硬/不一致 | [ ] 待确认 |

## 七、状态机
- 主流程：<草稿 → 待报价 → 已报价 → 已成交 → 已关闭>
- 异常支线：<报价全部超期失效 → 回到「待报价」并通知买方；被拒不改变状态>；无隐藏流转；现状不存在的状态（如「超时自动关闭」）如实标注。

## 八、业务事件与通知
| 事件 | 触发 | 触达对象 | 用户可见结果 |
|------|------|----------|--------------|
| <报价全部失效> | <最后一份有效报价到期> | <买方> | <回到「待报价」并提示重新发布> |

## 九、边界与异常场景
空态 / 加载中 / 成功 / 失败（校验·业务·权限·系统）/ 重复提交 / 并发 / 网络超时 / 部分失败——逐项写或标「不适用 + 原因」。

## 十、影响面
| 受影响功能 [[模块id]] | 影响原因 | 是否需同步修改 | 改什么 |
|----------------------|----------|----------------|--------|
|                      |          |                |        |

## 十一、前提条件与待确认项
- 操作前提（现状守卫，业务语义）：<仅认证供应商可报价>、<会员等级达到 2 级才返积分>……已确认的标「已确认」。
- [ ] <待确认 1>（证据：<业务事实描述，不写代码标识>）
- [ ] <待确认 2>

### 与用户确认记录
| 问题 | 用户拍板答案 | 状态 |
|------|--------------|------|
|      |              | 已确认 / 待确认 |

## 十二、实现前提条件（落地依赖——给实现方）
> 本节写给拿本文档去实现的人/AI：**要实现本需求，目标项目必须具备（或提供等价物）什么**。
> 只写业务域/数据/能力依赖；UI 框架、表/字段设计、接口风格、权限点命名、消息实现方案等由实现方自选，不构成前提。

| 类别 | 实现前提 | 为什么必须 | 缺失时的对策 |
|------|----------|-----------|--------------|
| 业务概念依赖 | <如：买家账号体系与「企业认证」概念> | <对应 R 规则/章节> | <等价替代或需业务拍板> |
| 数据前提 | <编号唯一生成与防重口径、存量状态映射等> | | |
| 能力前提 | <通知/消息渠道、审计与时间记录、金额精度与币种等> | | |
| 非功能前提 | <并发防双成交、幂等；现状不足处显式标为改进点> | | |

## 十三、一致性核对痕迹（结论节，证据在底稿）
- 已按《<日期>-<业务名>-一致性核对底稿.md》完成：正向溯源 ☑ / 反向覆盖 ☑ / 分组自查 ☑（未决项另注）。
- 标识扫描：交付文档不含原代码字段名/变量名/表名/索引名/接口路径/文件名 ☑。
- 自查结论：☑ 通过 / ⚠ 未决 N 项（已进十一）／ ✗ 未通过（未通过不得宣告完成）

## 附录
《<日期>-<业务名>-一致性核对底稿.md》内容：业务事实卡片、正/反向溯源表、分组自查明细、代码证据索引。
**底稿含代码内部标识，仅供一致性核对使用；并入需求文档正文属于违规。**
