# <YYYY-MM-DD>-<受影响业务逻辑名称>-实施计划

> 本文件是据完善文档产出的**分步实施计划**，由技能模式② 第二步生成，供模式③（`business-logic-execute`）执行。写作方法见 `references/mode2-improve.md`、验证分层见 `references/validation-levels.md`。
> **来源完善文档**：[<完善文档文件名>](../improve/<YYYY-MM-DD>-<名称>-<需求概述>.md)
> **对抗式审查**：<审查记录文件名>，结论：✅通过 / ⚠️带条件通过 / ❌未通过（由对抗式审查门产出，见 `mode2-improve.md` 第七节）

## 一、目标与范围
> 本次要落地的业务变更，对应完善文档的「目标业务逻辑」。

## 二、任务拆解（分步 + 依赖顺序 + 风险等级 + 视角标签）
> 每步尽量对应完善文档里某条目标逻辑，便于追溯与验证。**不估工时**（AI 执行无 hour 概念），改用**复杂度/风险等级 低/中/高** 标注哪些步骤要格外小心、要设检查点。标「高」或涉及迁移/删除/外部回调的步骤，做完先停下向用户汇报再继续。
> **视角标签**：每步标注对应哪个帽子——🎩产品（业务逻辑实现）、🎩项目（基础设施/发布/迁移）、🎩设计（UI/UX 落地）——确保三个视角都有步骤落地，没有遗漏。

| # | 步骤 | 涉及端/接口 | 依赖 | 风险 | 视角 | 验证层级 | 验证方式(看到什么算过) |
|---|------|-------------|------|------|------|----------|------------------------|
| 1 | 后端：下单接口加限购校验 | 后端 `/order/create` | — | 中 | 🎩产品 | L0+L1+L3 | 单测过 + Swagger 调用返回"已达上限" |
| 2 | 前端：失败展示「已达限购」 | 小程序 | 1 | 低 | 🎩设计 | L0+L4 | 真机下单超限看到提示 |
| 3 | 后台：新增限购配置菜单 | 后台 | 1 | 中 | 🎩产品 | L0+L4 | 后台配置后前端生效 |

## 二-B、UX 实现清单（🎩设计 —— 从完善文档「UX/UI 设计方案」拆出的具体前端任务）
> 这些任务要和业务逻辑改动一起排进上面的任务拆解表，不要单独放最后做。

| # | UX 任务 | 涉及页面/组件 | 依赖步骤 | 验证方式（在真机/浏览器上看到什么算过） |
|---|---------|-------------|---------|---------------------------------------|
| 1 | 加载态：列表加骨架屏 | 订单列表页 | 步骤 N | 慢网速下看到骨架屏，不是白屏 |
| 2 | 空态：无订单时显示引导入口 | 订单列表页 | 步骤 N | 新用户看到"去下单"引导按钮 |
| 3 | 确认弹窗：删除前二次确认 | 订单详情页 | 步骤 N | 点删除→弹窗出现→取消→没删；确定→删了 |
| … | … | … | … | … |

## 三、受影响功能的同步改动
> 完善文档「影响面分析」里标了「需同步修改」的 `[[id]]`，在这里落成具体步骤，收尾按 id 逐个核对是否真改了。

- [[inventory]] 库存：<具体改动>

## 四、验证与验收
> 层级定义见 `references/validation-levels.md`（本技能内置）。逐条对应完善文档目标。
- [ ] 各步按上表标注层级验证（不只是 build 通过）
- [ ] 边界/异常场景验证
- [ ] 受影响功能（`[[id]]` 清单）回归
- [ ] 多模块时：分模块验 + 跨模块 L4 联动验

## 五、风险与回滚预案
> 每个高风险步骤万一失败/做错怎么退。
- 隔离：改动大时在 git worktree 里做，坏了整体丢弃（执行阶段由 `business-logic-execute` 处理工作区隔离，计划阶段只需标注步骤风险）。
- 数据类改动：迁移前先备份 / 迁移脚本可逆。
- 连续卡住：停下，把现象记入本文档下方「受阻」，回报用户，不硬冲。

### 受阻记录（执行中回填）
- <步骤# / 现象 / 卡在哪>

---

**同步到项目白皮书的 md 文档对应的业务逻辑中**
> 计划执行完（验证过 + 审查无阻塞）后，必须把本次业务变更回写到 `docs/business-logic/<模块>.md` 对应章节（含被牵动的 `[[id]]` 模块），并更新根目录《业务逻辑白皮书》里该模块的状态与最近更新日期；在模块文档 frontmatter 记「关联计划 + 关联提交」做轻量溯源。同步核对标准见 `business-logic-execute`（模式③收尾）。
