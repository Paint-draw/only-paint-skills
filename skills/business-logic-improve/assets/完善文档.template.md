# <YYYY-MM-DD>-<受影响业务逻辑名称>-<需求概述>

> 本文件是「优化/完善某块业务逻辑」的**需求与影响面分析**，由技能模式② 第一步（配合本技能内置质询法 `references/grill-with-docs.md` 与用户确认后）产出。
> 定稿后，据本文件在 `docs/business-logic/plan/` 生成实施计划文档。写作方法见 `references/mode2-improve.md`。

## 〇、前置确认（动手前先落定）
- 现状模块文档：`docs/business-logic/<模块>.md`，当前状态 `<待补全/草稿/已核对>`。
  - 若非「已核对」→ 现状不可信，**先补现状再往下**。

## 一、为什么要改（动机）
> 当前业务逻辑哪里不够用 / 有什么问题 / 用户提出了什么新诉求。

## 二、原有业务逻辑（改之前是什么样）
> 照实描述现状，指向对应模块文档。缺失的现状先补清楚再往下写。

## 三、目标业务逻辑（改成什么样）
> 期望改成的完整业务路径：用户怎么操作、成功/失败/边界怎么展示、后台要不要加操作、后端规则怎么变、状态机新增/变更哪些分支。写到能直接照做的粒度。

## 四、需求决策清单（逐条确认，不适用写「不适用」）
| 决策点 | 结论 |
|--------|------|
| 目标行为（正向+失败+边界） | |
| 历史/存量数据怎么办、要不要数据迁移或回填 | |
| 兼容与发布：要不要停机、能否灰度/开关、旧接口旧字段保留还是废弃 | |
| 受影响的角色 / 端、后台要不要加菜单或审核 | |

## 五、非功能维度 + 体验维度评估（不涉及写「无影响」）
| 维度 | 评估 |
|------|------|
| 性能（慢 SQL / 索引 / 循环查询） | |
| 数据一致性（跨表跨服务写、事务、中间态） | |
| 幂等（重复触发/回调/重试 是否去重） | |
| 并发（抢占类操作的竞态怎么防） | |
| 用户体验（按必问清单 UX 12 条逐项过，每条要有具体方案） | |

## 五-B、UX/UI 设计方案（🎩设计 —— 每个页面/操作逐状态覆盖）
> 这是模式②的核心新增：不只写"用户看到什么"，而是把每个状态的设计方案写清楚，让模式③能直接照着实现。

### 涉及页面清单
| 页面/组件 | 所属端（后台/小程序/H5） | 是否新增 | 对应现有页面（如改旧页面） |
|-----------|------------------------|---------|--------------------------|
| | | | |

### 逐页面状态设计（每个页面都过一遍，不适用写「无此状态」）
**页面：<页面名>**
| 状态 | 设计方案（用什么组件/什么文案/什么交互） |
|------|------------------------------------------|
| 加载中 | |
| 空数据 | |
| 正常展示 | |
| 操作成功 | |
| 操作失败（业务错误） | |
| 网络/接口异常 | |
| 权限不足 | |
| 数据过期/失效 | |

### 交互细节
| 交互点 | 设计方案 |
|--------|---------|
| 不可逆操作确认（删除/支付/提交/退款…） | 弹窗标题+正文+按钮文案分别写清楚 |
| 表单校验 | 实时校验还是提交时校验？错误信息放字段旁还是顶部汇总？ |
| 操作可撤销/可回退 | 能不能撤销？能的话怎么撤销？不能的话有没有事前警告？ |
| 页面间跳转/返回 | 从哪里来、回哪里去？返回时要不要保留之前的状态（筛选/分页）？ |

### 文案与视觉一致性
| 检查项 | 结论 |
|--------|------|
| 新增/修改的文案和现有页面用词一致吗？ | |
| 按钮动词统一吗（保存/提交/确认不混用）？ | |
| 复用了现有 UI 组件吗？还是新写了样式？ | |
| 移动端可点击区域 ≥44px？关键信息不挡？ | |

## 六、影响面分析
> 每条受影响功能用 `[[模块id]]` 标注，作为贯穿到计划与收尾核对的追踪 key。这一栏必须和用户逐条确认过。

| 受影响功能 | 影响原因 | 是否需同步修改 | 需要同步修改的内容 |
|-----------|----------|----------------|--------------------|
| [[inventory]] 库存 | 下单扣库存逻辑变了 | 是 | 库存扣减时机调整 |
| [[coupon]] 优惠券 | … | 否 | — |

## 七、与现有架构的冲突（如有）
> 用户诉求和现有技术栈/数据模型打架时，在此记冲突点、为什么现状扛不住、可选路径+代价、用户拍板结论。没有写「无冲突」。

## 八、与用户确认记录（本技能内置质询法，见 `references/grill-with-docs.md`）
> 逐条质询后敲定的结论：术语是否一致、边界是否覆盖、受影响功能是否一起改、架构冲突怎么选。

- 确认点 1：<问题 → 用户拍板的答案>
- 确认点 2：<…>

## 九、三顶帽子复核（定稿前用三个视角分别过一遍）
> 每一项都问：🎩产品「业务对吗」→ 🎩项目「风险控了吗」→ 🎩设计「用户体验好吗」

| 决策/改动 | 🎩产品（业务价值/逻辑完备/影响面） | 🎩项目（风险/发布/回滚/依赖） | 🎩设计（交互/状态覆盖/一致性） |
|-----------|-----------------------------------|------------------------------|-------------------------------|
| | | | |

## 十、完成标准自检（全打勾才算定稿）
- [ ] 目标逻辑写到能直接照做（正向+失败+边界）
- [ ] 涉及状态机的，全分支覆盖
- [ ] 异常/边界逐项有说法
- [ ] 影响面每条都和用户确认过"是否同步改+改什么"
- [ ] 非功能五维（性能/一致性/幂等/并发/UX）都评估过
- [ ] UX 各状态设计方案逐页面填写完毕，不是空表
- [ ] 架构冲突（如有）已上报并有用户结论
- [ ] 三顶帽子复核表填写完毕，没有视角空白

## 十一、未决问题
- [ ] <还需人工拍板的点>
